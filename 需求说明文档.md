# harness_mini — AI Agent 编码工具 需求说明文档

| 项目 | 内容 |
| --- | --- |
| 项目名称 | harness_mini（暂定名） |
| 文档版本 | v0.8（修订：设置类弹窗统一规范——右上角关闭叉、禁用遮罩关闭、取消/应用/保存 统一操作条；程序设置改为侧边栏分组：模型设置 / 审批规则 / 数据目录） |
| 日期 | 2026-09-13 |
| 文档状态 | 待评审 |

---

## 1. 项目概述

### 1.1 背景

目标产品是一个 **AI Agent 编码工具**（桌面端），用户在本地工作区中通过自然语言与 AI 结对，完成日常编码工作：阅读/理解代码、修改文件、执行命令、搜索定位等。

产品演进分阶段进行：

- **第一期（本文档的设计范围）**：基础对话 + 日常编码能力，界面简洁，参照 ZCode 的布局与交互风格。
- **第二期（仅作架构预留，不在本文档做功能设计）**：定时任务（按计划自动触发 Agent 执行任务）。
- **第三期（仅作架构预留）**：多任务协调（多个 Agent 会话并行、编排、汇总）。

### 1.2 核心设计原则

1. **低内存占用是一票否决项**：所有技术选型优先考虑内存与包体，不引入重量级运行时。
2. **核心与界面分离**：Agent 运行时必须独立于 UI 存在（headless 可运行），否则第二期定时任务（无人值守）无法实现。
3. **会话是一等公民**：底层从第一天起按"多会话并发"设计，第一期 UI 只呈现单会话交互，不做功能阉割式返工。
4. **一切运行行为事件化、落库**：为后期的任务审计、定时触发、多任务协调提供统一的数据与事件基础。

### 1.3 名词约定

| 名词 | 含义 |
| --- | --- |
| 会话（Session） | 一次与 Agent 的完整对话上下文，绑定一个工作区 |
| Agent 运行时 | LLM 调用循环 + 工具执行的执行体，运行在 Rust 后端 |
| 工具（Tool） | Agent 可调用的原子能力，如读文件、写文件、执行命令 |
| 审批（Approval） | 高风险工具执行前向用户请求确认的交互 |
| 运行（Run） | Agent 被触发一次的执行记录；第一期触发方式只有手动 |
| 工作区（Workspace） | 会话绑定的本地目录，Agent 文件操作的默认根目录 |
| 项目（Project） | 侧栏分组实体，与工作区目录一一对应（工作区即项目）；会话保存时按目录自动归入 |

---

## 2. 需求分析

### 2.1 用户与场景

目标用户：需要 AI 辅助日常编码的开发者。

典型场景：

1. **问答/讲解**：询问某段代码的作用、某个报错的原因。
2. **代码修改**：让 Agent 修改一个功能、修复一个 bug，要求能看到改动内容（diff）并可控（可拒绝）。
3. **命令执行**：让 Agent 运行构建、测试、git 等命令，要求有审批与输出回显。
4. **代码检索**：让 Agent 在工作区内查找文件、搜索内容，回答"这个功能在哪个文件实现"。
5. **多轮迭代**：围绕同一问题连续多轮对话，Agent 能记住上下文持续工作。

### 2.2 功能需求清单（第一期）

按模块列出，P0 = 必须实现，P1 = 第一期尽量实现，P2 = 第一期不做、仅预留。

| 编号 | 模块 | 功能 | 优先级 |
| --- | --- | --- | --- |
| F1.1 | 模型接入 | 支持任意 OpenAI 兼容 API（自定义 Base URL + API Key + 模型名） | P0 |
| F1.2 | 模型接入 | 多套 Provider 配置（如 GLM、DeepSeek、本地 Ollama），可切换 | P1 |
| F1.3 | 模型接入 | 连接测试（设置页一键验证 Key 可用性） | P1 |
| F2.1 | 会话管理 | 新建会话、自动生成标题、重命名、删除 | P0 |
| F2.2 | 会话管理 | 会话历史列表、重启应用后恢复 | P0 |
| F2.3 | 会话管理 | 会话绑定工作区目录 | P0 |
| F2.4 | 会话管理 | 侧栏双视图：按列表 / 按项目；按项目时会话显示为项目下级节点 | P0 |
| F2.5 | 会话管理 | 工作区即项目：选择工作区即选择项目（已有项目 / 选择目录 / 不选择三选一）；会话保存时按工作区路径自动归入对应项目，项目不存在则自动创建（项目名取目录名）；已保存对话工作空间只读，不允许切换；项目支持新建（选择目录）、移除（不删除其下会话）、项目内新建会话、固定到顶部 | P0 |
| F2.6 | 会话管理 | 会话：归档 / 删除；会话与项目默认按最后对话时间倒序排列，置顶项目除外 | P0 |
| F2.7 | 会话管理 | 归档中心：侧栏设置入口之前，查看已归档会话列表，支持查看 / 取消归档 / 删除 | P0 |
| F2.8 | 会话管理 | 临时对话：新建会话不落库，首次实际发送消息才创建会话记录 | P0 |
| F2.9 | 项目设置 | 侧栏项目菜单「项目设置」弹窗（左侧菜单 + 右侧内容）：**项目约束**——Markdown 编辑 / 预览，保存于项目；该项目下每个新对话将其注入 system prompt（同一对话不重复注入、上下文截断不丢失、修改后同对话下一条消息生效）。**关联项目**——列表首行为当前项目（只读），可添加多个关联项目（选择目录 + Markdown 说明），支持编辑 / 删除；新对话同时注入关联说明，关联项目自身已设约束时一并注入；与本项目约束冲突时以关联项目为准。弹窗规范见 §4.2「设置类弹窗」 | P1 |
| F3.1 | 对话 | 流式输出（逐 token 渲染）、停止生成 | P0 |
| F3.2 | 对话 | Markdown 渲染、代码块语法高亮、一键复制 | P0 |
| F3.3 | 对话 | 多轮上下文、消息自动持久化 | P0 |
| F3.4 | 对话 | 运行中补充输入：运行期间可继续输入并回车，消息进入输入框上方的待执行列表，支持"引导"（立即介入当前对话、引导方向）与"删除"；未被引导的在当前对话结束后自动依次执行（参照 ZCode） | P0 |
| F3.5 | 对话 | 编辑并重新发送：最后一条用户消息可原地编辑，确认后作废其后消息与工具事件并重新运行（仅会话空闲时可用） | P0 |
| F4.1 | Agent 工具 | read_file / list_dir（只读） | P0 |
| F4.2 | Agent 工具 | write_file / edit_file（写入，展示 diff） | P0 |
| F4.3 | Agent 工具 | run_command（Shell 执行，输出回显，超时控制） | P0 |
| F4.4 | Agent 工具 | glob / grep（文件名与内容搜索） | P0 |
| F4.5 | Agent 工具 | todo 计划工具（Agent 维护任务清单并在 UI 呈现） | P1 |
| F5.1 | 权限审批 | 工具按风险分级：只读自动放行；写入/命令默认需审批 | P0 |
| F5.2 | 权限审批 | 审批交互：允许一次 / 本会话允许 / 拒绝（拒绝原因回传给 Agent） | P0 |
| F5.3 | 权限审批 | 按规则记忆（如某命令前缀、某路径写入永久放行），规则可管理 | P1 |
| F5.4 | 权限审批 | 访问模式设置：变更前确认（默认）/ 完全访问，全局默认 + 会话级快捷切换（参照 ZCode） | P0 |
| F6.1 | 上下文管理 | 历史消息组装、token 估算与用量显示 | P1 |
| F6.2 | 上下文管理 | 超长上下文的截断策略（摘要压缩预留接口，P2 实现完整版） | P1 |
| F7.1 | UI | 参照 ZCode 的三区布局（侧栏 / 对话区 / 输入区），深色主题为主 | P0 |
| F7.2 | UI | 工具调用以卡片形式内嵌对话流（参数、状态、结果、diff） | P0 |
| F7.3 | UI | 程序设置弹窗（左侧菜单分组：**模型设置**（模型厂商 + 运行参数）/ **审批规则** / **数据目录**；统一 取消 / 应用 / 保存 操作条，右上角关闭叉，不支持点击遮罩关闭） | P0 |
| F7.4 | UI | 系统托盘：关闭窗口默认隐藏到托盘继续运行；托盘左键显示主窗口，右键菜单（显示主窗口 / 退出） | P1 |
| F8.1 | 存储 | 会话、消息、工具事件、审批规则本地持久化（SQLite） | P0 |
| F8.2 | 存储 | API Key 加密存储（不明文入库） | P0 |

**第一期明确不做**（防止范围蔓延）：定时任务、多任务并行面板、MCP/插件系统、图片等多模态输入、代码库索引/向量检索、云端同步、账号体系、IDE 插件。

### 2.3 非功能需求

| 类别 | 要求 |
| --- | --- |
| 内存 | 空载（仅开窗口、无对话）常驻内存 **≤ 150MB**；10 轮长对话后 ≤ 300MB；消息列表虚拟化，内存不随历史长度线性增长 |
| 包体 | 安装包 ≤ 15MB（不含系统 WebView 组件） |
| 性能 | 冷启动首屏 ≤ 1s；流式输出不卡顿；工具结果超过 32KB 截断展示（全文保留在库中） |
| 平台 | Windows 10/11 x64 优先（开发环境即 Windows）；技术栈保证 macOS/Linux 可移植 |
| 安全 | 命令执行与文件写入默认审批制；文件工具路径默认限制在工作区内；高危命令模式（如 `rm -rf`、`format`）强制审批且不可永久放行；API Key 使用操作系统凭据保护加密存储 |
| 可靠性 | 崩溃/退出后重启可恢复全部会话历史；单次工具执行超时可配置（默认 120s），可手动终止子进程 |
| 隐私 | 对话与工作区数据仅存本地；网络仅访问用户配置的 LLM 端点 |

---

## 3. 总体技术方案

### 3.1 技术选型

#### 桌面框架：Tauri 2（结论：唯一推荐）

| 方案 | 空载内存（典型） | 包体 | Agent 生态适配 | 结论 |
| --- | --- | --- | --- | --- |
| Electron + Node | 200–400MB | 80–150MB | 好 | ✗ 内存不达标 |
| **Tauri 2（Rust + 系统 WebView）** | **60–120MB** | **3–10MB** | 好 | ✓ 采用 |
| Flutter Desktop | 100–200MB | 20–50MB | 中（FFi 桥接麻烦） | ✗ |
| Qt / PySide | 100MB+ / Python 运行时 | 大 | 中 | ✗ |
| 纯 TUI（终端） | <30MB | 极小 | 好 | ✗ 不满足 GUI 界面要求 |

补充说明：

- Windows 上 Tauri 使用系统 WebView2，运行时组件随 Win10/11 分发，多应用共享，实际增量内存更低。
- 打包时需检测 WebView2，缺失则引导安装（Tauri 官方 installer 支持）。

#### 技术栈明细

| 层 | 选型 | 理由 |
| --- | --- | --- |
| UI 框架 | React 18 + TypeScript + Vite | 生态成熟、类型安全；Vue 亦可，按团队熟悉度定 |
| 状态管理 | zustand | 极轻量（~1KB），避免 Redux 样板 |
| 样式 | Tailwind CSS | 快速搭建简洁界面 |
| Markdown 渲染 | react-markdown + remark-gfm | 流式渲染友好（增量渲染稳定） |
| 代码高亮 | highlight.js（按需注册常用语言） | 比 shiki 轻一个量级，满足"内存小" |
| 长列表 | @tanstack/react-virtual 或 virtua | 消息列表虚拟化，控制内存 |
| 桌面壳 | Tauri 2（Rust） | 见上 |
| 异步运行时 | tokio | Rust 事实标准，支撑多会话并发 |
| HTTP/SSE | reqwest + eventsource-stream | LLM 流式响应解析 |
| 存储 | rusqlite（bundled SQLite） | 单文件、零依赖、嵌入式，为后期任务/调度数据复用 |
| Key 加密 | keyring crate（Windows DPAPI） | 操作系统级凭据保护 |
| 进程执行 | tokio::process + 平台 shell | 命令执行、可 kill |
| 序列化 | serde / serde_json | 全链路 JSON |

#### 关键选型决策：Agent 核心放 Rust，不放前端、不用 Node Sidecar

- **不放 UI 层（WebView 内 TS）**：定时任务要求 Agent 在无窗口状态下被系统时钟唤醒执行，UI 层逻辑做不到无人值守。
- **不用 Node.js Sidecar**：多一个 Node 进程常驻 50–100MB，直接违背内存目标；且进程间通信增加复杂度。
- **Rust 内实现**：一个二进制、单进程（+WebView），内存最优；SSE、JSON、进程管理等基础设施均有成熟 crate，工程量可控。

> 兜底说明：若后续某能力在 Rust 生态缺失（如未来要跑复杂脚本型工具），再以"按需拉起、用完即退"的子进程方式扩展，不引入常驻运行时。

### 3.2 系统架构

```
┌────────────────────── UI 层（WebView / React）───────────────────────┐
│  侧栏(会话列表)   对话流(消息/工具卡/Diff卡/审批条)   输入区   设置页  │
│  职责：渲染 + 用户交互。不包含任何 Agent 业务逻辑。                  │
└───────────────▲────────────────Tauri IPC（command + event）──────────┘
                │ 命令下发：发送消息/审批结果/会话管理/设置读写
                │ 事件上行：token 增量、工具状态、审批请求、会话状态
┌───────────────┴────────────── Rust 核心层（headless 可运行）─────────┐
│                                                                      │
│  Application 应用编排层                                              │
│   ├─ SessionManager    会话生命周期、N 路并发（每会话一个 tokio task）│
│   ├─ ApprovalService   审批决策（策略 + 规则 + 请求用户确认）         │
│   ├─ EventBus          统一事件广播（UI 是订阅者之一，非唯一）        │
│   └─ Scheduler(预留)   二期挂载点：按计划创建 Run                     │
│                                                                      │
│  Domain 领域层                                                       │
│   ├─ AgentRuntime       Agent 主循环（见 §5.1）                      │
│   ├─ ContextManager     上下文组装 / token 估算 / 截断策略            │
│   └─ ToolRegistry       工具注册表（trait Tool，见 §5.2）             │
│                                                                      │
│  Infrastructure 基础设施层                                           │
│   ├─ LlmClient          OpenAI 兼容协议适配、SSE 流式解析             │
│   ├─ ToolExecutor       文件/命令/搜索的真正实现，路径与命令安全检查   │
│   ├─ Store(SQLite)      sessions / messages / tool_events / runs ...  │
│   └─ SecretStore        API Key 加密存取（DPAPI/keyring）             │
└──────────────────────────────────────────────────────────────────────┘
```

架构要点（对应"为二期三期留好口子"）：

1. **UI 只是 EventBus 的一个订阅者**。所有运行行为（token、工具状态、审批请求）以事件形式广播。二期"定时任务"触发一个会话时，同一个 UI 若打开就能实时旁观，不开也不影响执行——同一条管道，无需改造。
2. **每会话一个独立 tokio task**，SessionManager 天然支持 N 路并发。一期 UI 串行展示，三期"多任务协调"只是在 SessionManager 之上加编排层。
3. **Run（运行记录）抽象**：Agent 每次被触发即产生一条 Run（记录触发方式 `manual | scheduled`、状态、起止时间）。一期只有手动触发；二期定时任务 = 调度器按计划写入一条 `scheduled` 的 Run 并驱动同一套 AgentRuntime，**Agent 主循环零改动**。
4. **SQLite 为唯一持久化**：消息、工具事件、审批规则、运行记录全落库，天然支持后期审计、任务恢复、跨会话协调所需的历史数据。

### 3.3 进程与目录结构

进程模型：单进程 = Rust 主进程（含 Agent 核心）+ WebView 子系统（系统组件）。

代码仓库结构（建议）：

```
harness_mini/
├── src/                      # 前端 React
│   ├── App.tsx
│   ├── components/           # Sidebar / ChatStream / MessageItem / ToolCard /
│   │                         # DiffView / ApprovalBar / Composer / SettingsDialog
│   ├── stores/               # zustand: sessions, messages, settings, approvals
│   ├── ipc/                  # Tauri invoke/listen 封装，类型与 Rust 侧对齐
│   └── styles/
├── src-tauri/                # Rust 核心
│   └── src/
│       ├── main.rs
│       ├── ipc/              # Tauri command / event 定义
│       ├── app/              # SessionManager, ApprovalService, EventBus
│       ├── agent/            # AgentRuntime, ContextManager, 提示词
│       ├── tools/            # ToolRegistry + read/write/edit/command/glob/grep
│       ├── llm/              # OpenAI 兼容客户端、SSE、Provider 配置
│       ├── store/            # SQLite（迁移脚本内嵌）
│       └── secrets/          # keyring 封装
├── 需求说明文档.md
└── README.md
```

---

## 4. UI 设计（参照 ZCode）

### 4.1 整体布局

三区布局，深色主题为主（可预留浅色切换，P1）：

```
┌──────────┬──────────────────────────────────────────────┐
│          │  顶栏: [工作区路径 ▾]  [模型 ▾] [模式 ▾] [●] │
│  侧栏     ├──────────────────────────────────────────────┤
│          │                                              │
│[列表|项目]│   对话流（滚动区域，虚拟列表）                │
│ + 新会话  │                                              │
│ 📌项目A ▾│   ┌ 用户消息 ─────────────────────┐          │
│   · 会话1│   └───────────────────────────────┘          │
│ · 会话 C │   ┌ 助手消息（Markdown/代码高亮/复制）─┐       │
│          │   │  ┌ 工具卡片: edit_file  ✓已批准 ┐  │       │
│          │   │  │  main.rs  +3 −1  [查看diff] │  │       │
│          │   │  └─────────────────────────────┘  │       │
│          │   └───────────────────────────────────┘       │
│          │   ┌ 审批条: run_command `npm test`            │
│ ──────── │   │            [允许一次] [本会话允许] [拒绝]  │
│ 🗄 归档   ├──────────────────────────────────────────────┤
│ ⚙ 设置    │  待执行: · 消息1  [引导] [删除]              │
│          ├──────────────────────────────────────────────┤
│          │  输入区: 多行文本框            [发送/停止 ■▶] │
└──────────┴──────────────────────────────────────────────┘
```

- **侧栏**（可折叠）：顶部为视图切换（列表 / 项目，启动默认进入项目视图）与"新会话"按钮；中部为会话/项目树（标题 + 相对时间，Agent 运行中的会话在列表中显示绿色脉冲状态点与"运行中…"标识，便于区分多个并发对话的完成情况）；底部依次为"归档"与"设置"入口。会话删除需确认。
- **两种视图**：按列表 = 全部未归档会话平铺展示；按项目 = 顶部为可收起/展开的"项目"大分类（展开/收起箭头置于文字后方、仅悬停时显示，悬停分类尾部同时显示 + 新建项目，项目名取目录名），其下为各项目节点及项目内会话（项目节点可展开/折叠，会话缩进显示）；项目分类下方为"对话"分类（样式与项目分类一致），收纳未归入项目的纯对话，条目不缩进保持一级视觉，不存在纯对话时不显示该分类。
- **排序**：会话与项目均默认按"最后对话时间"倒序（项目取其下会话的最新时间）；项目可"固定到顶部"，置顶项目之间仍按时间倒序。
- **浮动操作按钮**：会话与项目条目默认不显示操作按钮，悬停时仅在尾部显示"…"更多按钮，点击弹出具体操作菜单（见 §4.2）。
- **顶栏**：当前工作区（未保存草稿点击弹出三选一：选择已有项目 / 选择目录 / 不选择工作区；已保存对话工作空间只读，仅展示并带 🔒 标识）、当前模型、访问模式切换（见 §4.3）、Agent 状态指示（空闲/思考/执行中）。
- **对话流**：用户消息、助手消息、工具卡片、审批条、系统错误统一为流内元素，按时间序排列。
- **输入区**：多行输入，Enter 发送 / **Alt+Enter 换行**（中文输入法组合态中按 Enter 不发送，避免选词误触发）；发送按钮在 Agent 运行中变为"停止"（中断当前 Run）。运行中输入新消息并回车不直接发送，而是进入输入框上方的**待执行列表**。
- **待执行列表**（输入框上方，有待执行消息时显示）：逐条展示运行期间补充的消息，每条提供两个操作——**引导**：该消息立即写入正在进行的对话、引导当前对话方向；**删除**：从列表中移除该消息。未被引导的消息在当前对话结束后**自动依次执行**（逐条触发新一轮运行）。

### 4.2 关键交互

| 交互 | 设计 |
| --- | --- |
| 工作区选择 | 仅未保存草稿可操作：顶栏工作区按钮弹出三选一——**选择已有项目**（项目列表，含其绑定目录）/ **选择目录**（系统目录选择框，保存对话时自动创建对应项目）/ **不选择工作区**（纯对话）。工作区即项目：会话保存时按工作区路径自动归入对应项目（不存在则创建，项目名取目录名）。**已保存对话工作空间只读**（🔒），不允许切换、清除或补绑工作区 |
| 设置类弹窗 | 「项目设置」「程序设置」统一规范：右上角关闭叉（✕）；**不支持点击遮罩关闭**，需手动点击关闭叉 / 取消 / 保存；底部统一操作条为 **取消**（丢弃改动并关闭）/ **应用**（只保存数据，不关闭窗口；无改动时禁用）/ **保存**（保存数据并关闭）。弹窗内容以草稿编辑，「应用 / 保存」时统一落库 |
| 流式输出 | token 增量追加渲染；未完成的消息尾部显示光标动画 |
| 运行中补充输入 | 运行中输入新消息并回车，消息进入输入框上方"待执行列表"；每条可 [引导]（立即注入当前运行、引导对话方向）或 [删除]（移除）；未引导的在当前运行结束后自动依次执行（机制见 §5.1） |
| 访问模式切换 | 顶栏下拉切换两种模式，会话级记忆；切到"完全访问"需一次性风险确认（见 §4.3） |
| 侧栏条目操作 | 悬停条目仅尾部显示"…"，点击展开操作菜单：会话 = 重命名 / 归档 / 删除；项目 = 新建对话 / 固定到顶部（或取消固定）/ 移除项目。移除项目不删除其下会话，会话自动移回顶层 |
| 归档中心 | 侧栏"归档"入口列出已归档会话（按最后对话时间倒序）；支持 查看（只读打开，继续对话需先取消归档）/ 取消归档 / 删除 |
| 临时对话 | 点击"新会话"仅在前端创建内存态草稿，不做任何写库；首次发送消息时才落库转正（生成标题、绑定当前工作区），未发送前退出或崩溃即自然丢弃 |
| 编辑并重新发送 | 会话空闲时，悬停最后一条用户消息显示浮动"编辑"按钮；消息气泡变为原地编辑框，可修改文本后确认（重新发送）或取消。重发 = 原消息内容更新为新文本，其后所有消息与工具事件作废移除，并触发新一轮运行；Agent 运行中不提供该操作 |
| 工具卡片 | 默认收起为一行（工具名、关键参数（路径/命令）、状态（待审批/执行中/成功/失败/被拒绝）），点击展开查看内容；展开后命令卡片展示输出（实时回显/超长折叠，可再展开），文件类卡片显示内容与 diff；审批条与 todo 清单保持常显 |
| 审批（仅"变更前确认"模式出现） | 审批条内联在对话流中出现位置，同时顶栏/侧栏给出醒目提示；提供 允许一次 / 本会话允许该规则 / 拒绝 三选项；拒绝时需可选填写原因，原因作为工具结果回传给 Agent 使其调整方案 |
| Diff 展示 | 统一 diff 语法着色，行内 +/−；超大 diff 折叠 |
| 停止 | 点击停止立即中断 LLM 流与当前工具子进程；已产生的消息与工具事件保留入库，Agent 停止 |
| 错误 | LLM 网络错误/超限/Key 失效等以系统消息卡片显示，附重试按钮 |

### 4.3 访问模式（参照 ZCode）

两种模式决定工具执行是否需要人工确认：

| 模式 | 行为 |
| --- | --- |
| 变更前确认（默认） | 只读工具自动执行；写入与命令执行按 §5.3 审批引擎逐项确认，可保存为规则减少打断 |
| 完全访问 | 所有工具（含命令执行、高危命令）自动放行，不出现审批条，Agent 连续执行不被打断 |

补充规则：

- 全局默认在设置页配置；每个会话可在顶栏单独切换，取值随会话记忆（`sessions.access_mode`）。
- 切换到"完全访问"时弹一次性风险确认（明确提示将跳过全部审批），确认后生效；切回"变更前确认"无需确认。
- 完全访问模式下工具卡片仍完整记录参数与结果（可审计），命中高危命令特征时卡片标红提示但不阻塞执行。
- 安全底线与模式无关：所有工具调用无论模式一律落库留痕；"完全访问"建议仅在可信项目与本地环境使用。

---

## 5. 核心机制设计

### 5.1 Agent 主循环

```
触发（用户发送消息 / 编辑重发 / 二期由调度器触发）
  └─ 创建 Run 落库（status=running）
  └─ 循环（受 max_steps 限制，默认 30，可配置）：
      1. 取出引导消息：仅当用户对待执行列表中的消息点了
         "引导"，将其注入上下文（role=user）并清除排队标记
      2. ContextManager 组装上下文：
         system prompt + 历史消息（含工具结果）+ 工具 schema 列表
      3. LlmClient 流式调用（携带 tools 定义）
      4. 若返回纯文本 → 写库、EventBus 推送 → Run 结束（done）
      5. 若返回 tool_calls → 逐个处理：
         a. 权限判定（ApprovalService，先看访问模式，见 §5.3）
         b. 放行 → ToolExecutor 执行（超时、输出截断、路径安全检查）
         c. 结果写库并作为 tool 消息进入上下文
         d. 回到下一轮循环
      6. 任一环节可被用户"停止"中断 → Run 置为 cancelled
```

**运行中补充输入（待执行队列）**：Run 活跃期间 `send_message` 不创建新 Run，而是将消息落库（`queued=1`）并经 EventBus 广播——消息进入输入框上方的待执行列表（不直接进入对话流），每条消息可被**引导**或**删除**：

- **引导**：`guide_message` 将该消息标记为待注入，主循环在下一轮次边界（步骤 1）将其写入上下文、消息转入对话流，Agent 在后续步骤中据此调整方向。注入不打断正在执行的工具，也不干扰进行中的审批（用户也可直接在审批时"拒绝并填写原因"即时纠偏）。
- **删除**：`delete_queued_message` 直接移除该排队消息（含库中记录）。
- **自动依次执行**：Run 结束时 SessionManager 检查待执行队列，若非空则取出最早一条、清除排队标记并以其触发新一轮 Run，如此往复直到队列为空；同一会话内的运行严格串行，保证执行顺序与队列一致。
- 引导时若 Run 恰已结束（竞态），该消息按普通发送处理（触发新一轮 Run）。"停止"只中断当前 Run，不影响待执行列表（队列继续自动依次执行）。

**编辑并重新发送**：仅会话空闲（无活跃 Run）时可用，针对**最后一条用户消息**（若对话以助手回复结尾，则为其前面那条用户消息）：

1. 确认重发后，原地更新该消息 `content`（保留消息 id 与 `seq`，会话内顺序不变）；
2. 该消息之后的所有消息与工具事件**作废并直接删除**（级联，一期不做软删除/分支树，保持存储与交互简单）；
3. 以该消息触发新一轮 Run，走正常主循环。

用于修正表述错误、补充条件后重新执行，或让 Agent 换一种思路重新处理同一问题。若此时待执行队列非空（如停止后的残留队列），重发完成后队列继续自动依次执行。

**系统提示词**（要点，文案可迭代）：角色定位（谨慎的编码助手）、工作区信息（根目录、平台、shell）、工具使用规范（先搜索再修改、修改前读取、避免越出工作区）、输出规范（改动后给出简短总结）。

### 5.2 工具系统

统一 trait 抽象，注册表驱动：

```rust
trait Tool {
    fn name(&self) -> &str;
    fn schema(&self) -> &JsonValue;        // JSON Schema，直接下发给 LLM
    fn risk(&self) -> RiskLevel;           // ReadOnly | Write | Execute
    async fn run(&self, args: JsonValue, ctx: &ToolContext) -> ToolResult;
}
```

第一期内置工具：

| 工具 | 风险级 | 说明 |
| --- | --- | --- |
| `read_file` | 只读 | 读取文本文件；二进制/超大文件特殊处理；默认限工作区内 |
| `list_dir` | 只读 | 目录树/列表 |
| `glob` | 只读 | 模式匹配查找文件名 |
| `grep` | 只读 | 正则内容搜索（walkdir + regex 实现，先不引入 ripgrep 二进制） |
| `write_file` | 写入 | 新建/整文件覆盖，UI 展示 diff |
| `edit_file` | 写入 | 精确串替换（old_string 唯一匹配 + replace），失败时返回明确错误让 Agent 重试；UI 展示 diff |
| `run_command` | 执行 | 在工作区以平台 shell 执行；输出实时回传；超时 kill；高危模式检测 |
| `todo`（P1） | 只读 | Agent 维护结构化任务清单，UI 渲染为清单组件 |

**路径安全**：只读/写入工具默认禁止解析到工作区外；越界路径必须走审批，且默认拒绝。**命令安全**：维护高危命令特征表（`rm -rf`、`del /s`、`format`、`mkfs` 等），命中则强制逐次审批，不允许"永久放行"。

### 5.3 审批策略引擎

**访问模式优先**（§4.3）：会话为"完全访问"时，所有工具调用自动放行并记录 `approval_scope='mode'`，不产生审批请求；为"变更前确认"（默认）时按以下顺序决策：

决策顺序：`永久规则 → 会话规则 → 工具风险级默认策略 → 兜底询问`。

- 规则数据结构：`{ kind: "command_prefix" | "path_write" | "tool", pattern, scope: "session" | "global" }`，落库存放，设置页可查看/删除。
- 审批是异步的：Agent task 挂起等待 UI 返回（或超时默认拒绝，防止无人值守卡死——这一特性同时是二期定时任务的必要行为）。

### 5.4 上下文管理

- token 估算：字符数 × 系数粗估（中文按 1 字 ≈ 0.6 token 校准），不引入 tokenizer 依赖，误差可接受；用量显示标注"估算"。
- 组装顺序：system（固定）→ 超限截断发生在"最旧的普通消息"，工具结果超长部分入库只保留截断版本。
- **压缩接口预留**：`trait ContextCompactor { async fn compact(history) -> history }`，一期提供"丢弃最旧"实现；二期实现"摘要压缩"（用同一 LLM 生成摘要替换历史）。

### 5.5 数据模型（SQLite）

第一期建表：

```sql
-- 项目（侧栏"按项目"视图；工作区即项目，path 为绑定的目录；支持置顶）
CREATE TABLE projects (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  path TEXT,                          -- 绑定的工作区目录；早期手动创建的项目可为空
  pinned INTEGER NOT NULL DEFAULT 0,   -- 1 = 固定到顶部
  created_at TEXT NOT NULL,
  constraints TEXT NOT NULL DEFAULT '' -- 项目约束（Markdown）；该项目下新对话注入 system prompt
);

-- 关联项目（项目设置中对另一目录的引用 + 说明；只存路径不存对方 id，
-- 对方项目被移除后目录关系仍成立；注入时按路径只读解析对方项目的自身约束）
CREATE TABLE project_links (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,           -- 归属（当前）项目
  path TEXT NOT NULL,                 -- 关联项目目录
  description TEXT NOT NULL DEFAULT '', -- 关联说明（Markdown）
  created_at TEXT NOT NULL
);

-- 会话
CREATE TABLE sessions (
  id TEXT PRIMARY KEY,            -- uuid
  title TEXT NOT NULL,
  workspace_path TEXT NOT NULL,
  provider TEXT, model TEXT,
  access_mode TEXT,               -- confirm | full_access；空则用全局默认
  project_id TEXT,                -- 所属项目；空 = 未分组（列表视图顶层）
  status TEXT NOT NULL DEFAULT 'active',   -- active | archived（归档保留数据）
  last_message_at TEXT,           -- 最后对话时间，侧栏默认排序依据
  created_at TEXT NOT NULL, updated_at TEXT NOT NULL
);

-- 消息（含工具调用消息，role: user|assistant|tool|system）
CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  run_id TEXT,
  seq INTEGER NOT NULL,           -- 会话内顺序
  role TEXT NOT NULL,
  queued INTEGER NOT NULL DEFAULT 0, -- 运行中补充消息：注入 Agent 前为 1
  content TEXT,                   -- 文本内容
  tool_calls_json TEXT,           -- assistant 的工具调用请求
  tool_call_id TEXT,              -- tool 角色消息对应调用 id
  usage_json TEXT,                -- token 用量
  created_at TEXT NOT NULL
);

-- 工具执行事件（审批、状态跟踪、卡片渲染的数据源）
CREATE TABLE tool_events (
  id TEXT PRIMARY KEY,
  message_id TEXT NOT NULL REFERENCES messages(id) ON DELETE CASCADE,
  tool_name TEXT NOT NULL,
  params_json TEXT NOT NULL,
  result_text TEXT,               -- 截断后的回显全文仍存 messages
  status TEXT NOT NULL,           -- pending_approval|running|success|failed|denied|timeout
  approval_scope TEXT,            -- none|once|session|rule
  created_at TEXT NOT NULL
);

-- 运行记录（为二期定时任务准备；一期只有 manual 触发）
CREATE TABLE runs (
  id TEXT PRIMARY KEY,
  session_id TEXT NOT NULL,
  trigger_type TEXT NOT NULL DEFAULT 'manual',  -- manual | scheduled(二期)
  status TEXT NOT NULL,           -- running|done|cancelled|failed
  started_at TEXT NOT NULL, ended_at TEXT
);

-- 审批规则
CREATE TABLE approval_rules (
  id TEXT PRIMARY KEY,
  kind TEXT NOT NULL,             -- command_prefix|path_write|tool
  pattern TEXT NOT NULL,
  scope TEXT NOT NULL,            -- global
  created_at TEXT NOT NULL
);

-- 二期预留表（一期不建，仅锁定 schema 设计方向）：
-- jobs(id, name, cron_expr, prompt, session_template_json, enabled, next_run_at, last_run_id)
```

**会话与项目生命周期补充**：

- **临时对话**：点击"新会话"仅在前端创建内存草稿，不产生任何写库行为；首次 `send_message(null, …)` 时才 INSERT sessions 并触发 Run（临时对话转正），未发送前退出/崩溃自然丢弃，符合"不做实际保存"的预期。
- **工作区即项目**：会话落库时按工作区路径查找项目并自动归入；不存在则以目录名自动创建一条项目数据（projects.path 记录目录）。路径匹配忽略大小写与分隔符差异（Windows）。
- **工作区只读**：会话落库（首次发送转正）后工作空间即固定为只读，顶栏工作区控件仅作展示（🔒），不允许切换、清除或补绑工作区；仅未保存草稿可选择工作区。`set_session_workspace` 对已存在会话一律返回错误（后端守卫）。
- **归档**：`status='archived'`，不参与侧栏主列表与常规查询；归档中心可恢复（`active`）或删除（级联删除消息与工具事件）。
- **移除项目**：仅将其下会话 `project_id` 置空（移回顶层），不删除会话数据。
- **编辑重发**：原地更新该用户消息 `content`，`seq` 不变；其后消息与工具事件直接删除（一期无软删除/分支树）。
```

### 5.6 IPC 接口草案（Tauri）

前端 → 后端（command）：

```
send_message(session_id | null, text) -- null：临时对话转正（落库）后触发；Run 活跃时自动进入待执行队列
guide_message(session_id, message_id) -- 引导：立即注入当前运行
delete_queued_message(message_id)     -- 从待执行列表移除
edit_and_resend(session_id, message_id, new_text)  -- 编辑重发：更新消息、作废其后消息并重新运行
set_session_mode(session_id, mode)    -- mode: confirm | full_access
respond_approval(event_id, decision: allow_once|allow_session|deny, reason?)
create_session(workspace_path) / rename_session / delete_session / list_sessions
list_projects / create_project(name, path?) / remove_project(id) / set_project_pinned(id, pinned)
set_session_workspace(id, path)  -- 守卫命令：已保存对话工作空间只读，对已存在会话一律返回错误；工作区仅在首次发送转正时确定
set_session_project(id, project_id?)  -- 仅调整会话所属项目（用于绑定未带目录的项目）
set_project_constraints(id, constraints)  -- 保存项目约束（Markdown）
list_project_links(project_id) / add_project_link(project_id, path, description)
update_project_link(id, path, description) / delete_project_link(id)
archive_session(id) / unarchive_session(id) / list_archived()
get_messages(session_id, before_seq?, limit)
stop_run(session_id)
get_settings / set_settings / test_provider(provider_cfg)
```

后端 → 前端（event，均携带 session_id）：

```
run:status        { session_id, run_id, status }
message:delta     { session_id, message_id, delta_text }
message:final     { session_id, message_id, content, usage, queued? }  // 用户消息落库（含 queued 标记）与助手消息完成均经此事件
queue:update      { session_id, items: [{ message_id, text }] }        // 待执行列表变化（入队/引导/删除/自动消费）
session:update    { session_id, title, last_message_at, project_id, status }  // 侧栏排序/分组/归档状态联动
tool:update       { session_id, event_id, status, params, result_preview }
approval:request  { session_id, event_id, tool_name, params, risk }   // 前端弹审批条
error             { session_id, kind, message }
```

---

## 6. 面向后续版本的架构预留（只说明落点，不做功能设计）

| 后续能力 | 一期已具备的基础 | 二/三期需要新增的部分 |
| --- | --- | --- |
| 定时任务 | Run 抽象含 `trigger_type`；AgentRuntime 无头可运行；审批可超时默认拒绝；SQLite 统一存储；托盘常驻已实现（关闭窗口隐藏到托盘） | `jobs` 表 + Scheduler 模块（cron 解析、错过补偿策略）；开机自启与托盘通知；任务执行结果通知 UI |
| 多任务协调 | SessionManager 天然多会话并发；EventBus 可同时承载 N 路会话事件；会话隔离的上下文与工作区 | 编排器（任务拆分/派发/汇总，可作为特殊工具或上层模块）；多会话并行面板 UI；跨会话通信协议 |
| 插件化工具（可选远期） | trait Tool 注册表，工具即数据（JSON Schema） | 动态加载机制（子进程工具/WASM），风险分级同样适用 |

约束现有实现的红线（开发时必须遵守，否则二期返工）：

1. UI 层禁止出现业务逻辑，一切 Agent 行为在 Rust 核心；
2. 不得假设"当前只有一个会话在运行"；
3. 所有状态变更必须经 EventBus 广播并落库，不允许仅存前端内存。

---

## 7. 开发里程碑

| 阶段 | 内容 | 验收标准 |
| --- | --- | --- |
| M1 骨架（约 1 周） | Tauri 工程搭建、三区布局静态 UI、SQLite 接入与建表、设置页 + Provider 配置 + 连接测试 | 可配置 API 并连通；窗口空载内存 ≤150MB |
| M2 对话（约 1 周） | 流式对话、会话管理（双视图/项目/归档/临时对话/排序）、消息持久化与恢复、最后一条消息编辑重发、Markdown/高亮/复制、停止 | 断电重启后历史完整；流式渲染流畅 |
| M3 Agent 化（约 2 周） | ToolRegistry 与全部 P0 工具、访问模式与审批引擎/审批条、待执行列表（引导/删除/自动依次执行）、Diff 视图、命令执行回显 | 端到端完成"改 bug 并跑测试"场景；待执行列表的引导、删除、自动依次执行行为正确 |
| M4 打磨（约 1 周） | 上下文截断、todo 工具、审批规则管理、错误处理、打包安装器、性能与内存验证 | 指标全部达 §2.3 验收 |
| M5（后续版本） | 定时任务 → 多任务协调（另行需求文档） | — |

## 8. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| Rust 实现 Agent 循环开发效率低于 TS | 排期 | SSE 解析、JSON、进程管理均有成熟 crate；核心循环逻辑简单（一重循环+分支），风险集中在边角处理；M3 前置技术预研 |
| edit_file 串匹配失败率高 | Agent 改码体验差 | 强约束"先 read 再 edit"；匹配失败返回上下文冲突错误引导 Agent 重读；保留 write_file 整写兜底 |
| 各 Provider 工具调用格式差异 | 兼容性 | LlmClient 内做协议适配层，仅暴露 OpenAI tools 格式；连接测试覆盖 tools 能力验证 |
| WebView2 版本差异导致 UI 兼容问题 | 展示异常 | 锁定常规 CSS 特性；打包检测 WebView2 版本 |
| 长会话内存增长 | 内存指标超标 | 消息虚拟列表 + 按需从 SQLite 分页加载 + 渲染层不持有完整历史 |
| 高危命令误放行 | 数据安全 | 变更前确认模式下特征表强制逐次审批、不可永久放行；完全访问模式下高危命令卡片标红警示并完整留痕；文件工具路径白名单默认最小化 |
| 运行中补充消息与工具执行竞态 | 上下文错乱 | 引导注入只发生在主循环轮次边界，不打断进行中的工具；排队消息在运行结束后由 SessionManager 严格串行依次消费；消息先落库后注入，崩溃后可恢复 |

---

## 9. 验收清单（第一期）

- [ ] 空载内存 ≤150MB，安装包 ≤15MB，冷启动首屏 ≤1s
- [ ] 可配置任意 OpenAI 兼容端点并流式对话，可停止、可重试
- [ ] 会话增删改查、重启恢复、绑定工作区
- [ ] 侧栏双视图：列表/项目切换、项目下级会话、固定到顶部、按最后对话时间倒序
- [ ] 归档中心：归档、取消归档、删除、只读查看归档会话
- [ ] 临时对话：未发送消息前不落库（退出即丢弃），发送首条消息后转正
- [ ] 编辑重发：修改最后一条用户消息后，其后消息与工具事件作废、Agent 重新运行；运行中该操作不可用
- [ ] Agent 可完成端到端编码任务：定位文件 → 阅读代码 → 修改（diff 可见）→ 执行测试（输出可见）→ 总结
- [ ] 运行中补充消息：待执行列表显示于输入框上方，支持引导（立即介入当前对话）/删除；未引导的在当前对话结束后自动依次执行
- [ ] 访问模式：变更前确认逐项审批；完全访问全自动放行、卡片留痕、开启时有一次风险确认
- [ ] 键位：Enter 发送、Alt+Enter 换行、输入法组合态回车不误发送
- [ ] 只读工具免审批；写入/命令默认审批；拒绝后 Agent 可获知原因并调整；高危命令强制逐次审批
- [ ] API Key 加密存储；对话数据全部本地化
- [ ] 代码中不存在"单会话假设"（架构评审项）
